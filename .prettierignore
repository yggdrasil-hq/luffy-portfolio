# Harness-managed files — the Helm chart templates use Go-template syntax that
# the YAML parser cannot handle, and test reports are generated artifacts.
.yggdrasil/

# Next.js dev-server-managed agent files (re-generated by `next dev`; must keep
# the exact managed block byte-for-byte, so they are excluded from formatting).
CLAUDE.md
AGENTS.md

# Build outputs
node_modules/
.next/