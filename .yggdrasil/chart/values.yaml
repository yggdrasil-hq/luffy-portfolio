replicaCount: 1

image:
  repository: luffy-portfolio
  tag: latest

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

# CI-managed checksum of the Dockerfile to trigger rollouts when the image changes.
dockerfileChecksum: ""
